// Database schema lives here and only here — this package owns the shape of
// stored data. Add your ORM (e.g. drizzle-orm) and driver when the project
// picks a database; the browser never imports this package.

export {}
